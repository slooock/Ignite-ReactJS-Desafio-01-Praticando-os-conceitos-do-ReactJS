import styled from 'styled-components'

export const Container = styled.section`
  margin-top: 1.5rem;
  display: grid;
  grid-template-columns: 1fr;
  grid-gap: 10px;
  height: 100%;
  overflow-y: auto;
`
